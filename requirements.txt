pandas>=2.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
xgboost>=2.0
eurostat>=1.0
world_bank_data>=0.1
shap>=0.42
jupyter>=1.0
ipykernel>=6.0
# ── Deep learning (only for notebook 05d) ────────────────────────────────
# CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
# GPU (CUDA 12.x): pip install torch --index-url https://download.pytorch.org/whl/cu121
